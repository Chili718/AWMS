/* var navContainers = document.querySelectorAll(".navButtonContainer");

//console.log(navContainers);

setTimeout(() => {
    
    navContainers.forEach(button => {

        button.style.opacity = 1;
        button.style.animation = '';
        console.log('here');

    });

}, 8000); */